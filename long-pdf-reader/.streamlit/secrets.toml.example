# Copy this file to `.streamlit/secrets.toml` (which is gitignored)
# and fill in your real values. Never commit real secrets.

OPENAI_API_KEY = "your-openai-api-key"
APP_PASSWORD = "your-private-password"
